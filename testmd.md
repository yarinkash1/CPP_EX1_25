 
במערכת Moodle יש להגיש **קובץ טקסט (`submission.txt`)** המכיל 3 שורות בפורמט הבא:

1. **תעודת זהות** – מספר תעודת הזהות של הסטודנט.
2. **קישור להגשה** – קישור למאגר ה-GitHub שבו נמצא הפרויקט.
3. **פרטי ה-commit האחרון** – המחרוזת המזהה של ה-commit האחרון (`commit hash`) 

## 📌 דוגמה לקובץ הגשה תקין:
```
123456789
https://github.com/example-user/graph-assignment
e3f1c1a 
```

## 📂 דרישות נוספות:
- יש לוודא שה-repository ציבורי, או לתת גישה למתרגלים אם מדובר במאגר פרטי.
- הפרויקט חייב לכלול את הקבצים הבאים:

  - 📌 **קובצי קוד מקור (`.h` ו- `.cpp`)** של המחלקות `Graph` ו- `Algorithms`.
  - ✅ **קובץ `Test.cpp`** עם בדיקות יחידה בעזרת `doctest`.
  - ✅ **קובץ `Demo.cpp`** להדגמת הפונקציונליות.
  - ⚙️ **קובץ `Makefile`** המאפשר הרצה נוחה של הקוד.
  - 📜 **קובץ `README.md`** עם הסבר על המבנה וההנחיות לשימוש.
  - 📝 **(רשות) דוח קצר** (`report.pdf` או `report.md`) המסביר את גישת המימוש, אתגרים וניתוח זמן ריצה.

---

 
